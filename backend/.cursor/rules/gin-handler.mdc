---
description: "Шаблон для нового обработчика (handler) в Gin"
type: "manual"
---

Создай новый обработчик для Gin со следующей структурой. Замени `Entity` на имя соответствующей сущности.

```go
package {{ .PackageName }} // e.g. user

import (
	"net/http"

	"github.com/gin-gonic/gin"
)

// GetEntity godoc
// @Summary      Get an entity
// @Description  get entity by ID
// @Tags         entity
// @Accept       json
// @Produce      json
// @Param        id   path      int  true  "Entity ID"
// @Success      200  {object}  models.Entity
// @Failure      400  {object}  httputil.HTTPError
// @Failure      404  {object}  httputil.HTTPError
// @Failure      500  {object}  httputil.HTTPError
// @Router       /entity/{id} [get]
func GetEntity(c *gin.Context) {
	// Your handler logic here
	c.JSON(http.StatusOK, gin.H{"message": "This is a placeholder for GetEntity"})
}

func CreateEntity(c *gin.Context) {
	// Your handler logic here
	c.JSON(http.StatusCreated, gin.H{"message": "This is a placeholder for CreateEntity"})
}
```

Не забудь добавить маршруты для новых обработчиков в `routes.go`.